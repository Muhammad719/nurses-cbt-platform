"use client"
import { useEffect, useState } from "react"
import { Download, X } from "lucide-react"
import { Button } from "@/components/ui/button"
export function PwaRegister(){const [prompt,setPrompt]=useState<any>(null);const [installed,setInstalled]=useState(false);useEffect(()=>{if(!("serviceWorker" in navigator))return;navigator.serviceWorker.register("/sw.js").catch(()=>{});const before=(e:any)=>{e.preventDefault();setPrompt(e)};const done=()=>{setInstalled(true);setPrompt(null)};window.addEventListener("beforeinstallprompt",before);window.addEventListener("appinstalled",done);return()=>{window.removeEventListener("beforeinstallprompt",before);window.removeEventListener("appinstalled",done)}},[]);if(!prompt||installed)return null;return <div className="fixed bottom-4 left-4 right-4 z-50 mx-auto flex max-w-md items-center gap-3 rounded-2xl border bg-background p-3 shadow-xl"><div className="flex h-9 w-9 shrink-0 items-center justify-center rounded-xl bg-primary/10 text-primary"><Download className="h-4 w-4"/></div><div className="min-w-0 flex-1"><p className="text-sm font-semibold">Install Examly</p><p className="text-xs text-muted-foreground">Add the CBT platform to your device.</p></div><Button size="sm" onClick={async()=>{await prompt.prompt();setPrompt(null)}}>Install</Button><Button size="icon" variant="ghost" onClick={()=>setPrompt(null)} aria-label="Dismiss"><X className="h-4 w-4"/></Button></div>}
